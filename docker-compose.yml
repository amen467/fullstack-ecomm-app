version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: ecomm_postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-ecomm_dev}
      POSTGRES_USER: ${POSTGRES_USER:-ecomm_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-ecomm_pass}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-ecomm_user}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
